# Инструкция по настройке GitHub репозитория

## Шаги для загрузки проекта на GitHub

### 1. Создайте репозиторий на GitHub

1. Перейдите на [GitHub](https://github.com)
2. Нажмите "New repository"
3. Название: `Lc-0_Chess` (или другое по вашему выбору)
4. Описание: "Красивое шахматное приложение на WPF с ИИ на базе Lc0"
5. Выберите **Public** или **Private**
6. **НЕ** добавляйте README, .gitignore или LICENSE (они уже есть в проекте)
7. Нажмите "Create repository"

### 2. Обновите README.md

✅ `README.md` уже обновлен с вашим GitHub username  
✅ `Installer/installer.iss` уже обновлен с вашим GitHub username

### 4. Инициализируйте Git (если еще не сделано)

```bash
# Если репозиторий еще не инициализирован
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit: Sakura Chess application"
```

### 5. Подключите удаленный репозиторий

```bash
# Используйте ваш GitHub username
git remote add origin https://github.com/D4cLoves/Lc-0_Chess.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Загрузите код на GitHub
git push -u origin main
```

### 6. Создайте первый релиз

#### Способ 1: Автоматический (через теги)

```bash
# Создайте тег
git tag v1.0.0

# Загрузите тег на GitHub
git push origin v1.0.0
```

GitHub Actions автоматически создаст релиз!

#### Способ 2: Ручной

1. Перейдите в раздел **Releases** на GitHub
2. Нажмите **"Draft a new release"**
3. Выберите тег `v1.0.0` (или создайте новый)
4. Заголовок: `Sakura Chess v1.0.0`
5. Описание:
   ```markdown
   ## Первый релиз! 🎉
   
   ### Особенности
   - Игра против ИИ на базе Lc0
   - Современный интерфейс Material Design
   - Поддержка различных временных контролей
   - Анализ позиции в реальном времени
   - Режимы: Classic и Chess960
   
   ### Установка
   1. Скачайте `SakuraChessSetup.exe`
   2. Запустите установщик
   3. Наслаждайтесь игрой!
   ```
6. Прикрепите `Installer/Output/SakuraChessSetup.exe`
7. Нажмите **"Publish release"**

## Что уже настроено

✅ `.gitignore` - исключает временные файлы  
✅ `README.md` - красивое описание проекта  
✅ `LICENSE` - MIT лицензия  
✅ `.github/workflows/release.yml` - автоматическая сборка релизов  
✅ `.github/ISSUE_TEMPLATE/` - шаблоны для issues  
✅ `.gitattributes` - правильная обработка файлов  
✅ `RELEASE.md` - инструкция по созданию релизов  

## Дополнительные рекомендации

### Добавьте скриншоты

1. Сделайте скриншоты вашего приложения
2. Сохраните их в папку `docs/screenshots/` или `Images/screenshots/`
3. Обновите README.md, добавив ссылки на скриншоты

### Настройте GitHub Pages (опционально)

Если хотите создать сайт для проекта:
1. Перейдите в Settings → Pages
2. Выберите источник: `main` branch, `/docs` folder
3. Создайте папку `docs/` с `index.md`

### Добавьте badges в README

После создания репозитория GitHub автоматически предоставит ссылки для badges:
- Build status
- Release version
- License

## Полезные команды Git

```bash
# Проверить статус
git status

# Добавить все изменения
git add .

# Создать коммит
git commit -m "Описание изменений"

# Загрузить на GitHub
git push

# Создать новую ветку
git checkout -b feature/новая-функция

# Переключиться на ветку
git checkout main
```

## Готово! 🎉

Ваш проект теперь на GitHub и готов к использованию!

